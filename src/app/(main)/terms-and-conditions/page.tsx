import TermsAndConditions from "@/src/components/TermsAndConditions";

const page = () => {
    return (
        <div>
            <TermsAndConditions />
        </div>
    );
};

export default page;